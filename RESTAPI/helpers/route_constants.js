module.exports.ROUTE_CONSTANTS = {
    DEFAULT : "/",
    GET_PRODUCTS : "/products",
    GET_LOGIN : "/login",
    GET_REGISTER : "/register",
    GET_FILE: "/file",       //new constant added.
    POST_REGISTER:"/register",
    POST_LOGIN: "/login"  // now we are adding another constant
}

// "files" named folder is created and demo.pdf is added in it.